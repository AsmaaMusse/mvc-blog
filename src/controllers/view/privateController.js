const { Blog, User } = require("../../models");

const renderDashboard = async () => {};

const renderBlogForm = async () => {};

const renderEditBlogForm = async () => {};

module.exports = {
  renderDashboard,
  renderBlogForm,
  renderEditBlogForm,
};
